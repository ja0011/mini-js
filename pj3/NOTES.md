# Notes — `pj3`

**`index.html` is still empty** — nothing to document yet.

When you add markup (and any CSS/JS), update this file like `pj1/NOTES.md` / `pj2/NOTES.md`: short bullets for each tag or function, then a **Current file** code block that matches the real sources.

Open **`pj3`** as the Cursor folder while you build here; preview **`index.html`** once it has content.
